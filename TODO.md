* GENERAL

* GUI
- add a mechanism to print messages issued by the compiler server (?)
- add a cache mechanism to avoid recomputation of diagram IOs (frequent and costly)
- within dynamic panels, put `Add` and `Clear` buttons at the bottom, not top
- in the transition edit panel, add a combo for selecting side (N,E,W,S) ?
- add a clickable region in the diagram panel to access local variables (more intuitive than right-click) ?
- use json format for options save file
- add sized ints
- add type_coercions in accepted expressions
- Allow attachement of priorities to transitions
- VHDL and SystemC syntax highlighters
- Export to SCXML ?

* BUILD
- add graphviz and gtkwave to dep pkgs in debian dist

* TOOLS

* DOC
- update the section *Trouble Shooting* in `doc/Using.md`
